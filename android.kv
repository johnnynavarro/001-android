ScreenManager:
    Tela1:
    Tela2:
    Tela3:
    Tela4:
    Tela5:
    Tela6:
    Tela7:

<Default@Screen>:

    MDFloatLayout:
        canvas:
            Color:
                rgba: app.theme_cls.primary_color
            Rectangle:
                size: self.width-0, self.height-10
                pos: 0, 900
                #valing: 'top'
        Image:
            orientation: 'vertical'
            size_hint: 0.2, 0.2
            source: 'inop.png'
            pos_hint: {'center_x':0.07, 'center_y': 0.94}

        MDLabel:
            text: "Não Soube/Não Opinou"
            pos_hint: {'center_x':0.6, 'center_y':0.17}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.17}
            on_press:



<Botao@MDFillRoundFlatButton>:
    pos_hint: {'center_x':0.80,'center_y':0.1}


<Pergunta@MDToolbar>:
    pos_hint: {'center_x':0.5, 'center_y': 0.85}


<Opcao@MDCheckbox>:
    group: 'grupo'
    size_hint: None, None
    size: dp(40), dp (40)
#    pos_hint: {'center_x':0.8, 'center_y':0.8}



<CaixaTexto@MDTextField>:
    hint_text: "Name"
    mode: "rectangle"
    size_hint: 0.75, None
    icon_right: "arrow-down-drop-circle-outline"
    icon_right_color: app.theme_cls.primary_color

<Tela1>:

    name: 'tela1'
    ScrollView:
        MDBoxLayout:
            cols: 1
            id: teste
            orientation: 'vertical'
            size_hint_y: None
            height: self.minimum_height

    Default:
        Pergunta:
            title: "Dia 15 de novembro teremos eleições para prefeito em quem você votaria para prefeito neste dia?"


        Botao:
            text: 'próximo'
            on_press: root.manager.current = 'tela2'

        MDLabel:
            text: 'Candidato A'
            pos_hint: {'center_x':0.6, 'center_y':0.75}
            size_hint_y: None
            height: 200

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.75}
            size_hint_y: None
            height: 200
            on_press:
                value = self.active
                if value: True
                root.prefeito = 'Candidato A'
                root.imprimir()

        MDLabel:
            text: 'Candidato B'
            size_hint_y: None
            height: 200
            pos_hint: {'center_x':0.6, 'center_y':0.65}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.65}

        MDLabel:
            text: 'Candidato C'
            size_hint_y: None
            height: 200
            pos_hint: {'center_x':0.6, 'center_y':0.55}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.55}
            size_hint_y: None
            height: 200

        MDLabel:
            text: 'Candidato D'
            size_hint_y: None
            height: 200
            pos_hint: {'center_x':0.6, 'center_y':0.45}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.45}

        MDLabel:
            text: 'Candidato E'
            pos_hint: {'center_x':0.6, 'center_y':0.35}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.35}

        MDLabel:
            text: 'Candidato F'
            pos_hint: {'center_x':0.6, 'center_y':0.25}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.25}

<Tela2>:

    name: 'tela2'
    Default:
        Pergunta:
            title: 'Quais os principais problemas da sua localidade(sede e povoados)?'
        Botao:
            text: 'próximo'
            on_press: root.manager.current = 'tela3'
        CaixaTexto:
            pos_hint: {'center_x':0.5, 'center_y':0.5}
<Tela3>:

    name: 'tela3'
    Default:
        Pergunta:
            title: 'Dos candidatos cidatos algum deles o Sr(a), Não Votaria de JEITO NENHUM?'

#        CaixaTexto:
#            pos_hint: {'center_x':0.5, 'center_y':0.5}
        MDLabel:
            text: 'Candidato A'
            pos_hint: {'center_x':0.6, 'center_y':0.75}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.75}


        MDLabel:
            text: 'Candidato B'
            pos_hint: {'center_x':0.6, 'center_y':0.65}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.65}

        MDLabel:
            text: 'Candidato C'
            pos_hint: {'center_x':0.6, 'center_y':0.55}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.55}

        MDLabel:
            text: 'Candidato D'
            pos_hint: {'center_x':0.6, 'center_y':0.45}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.45}

        MDLabel:
            text: 'Candidato E'
            pos_hint: {'center_x':0.6, 'center_y':0.35}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.35}

        MDLabel:
            text: 'Candidato F'
            pos_hint: {'center_x':0.6, 'center_y':0.25}

        Opcao:
            pos_hint: {'center_x':0.8, 'center_y':0.25}

        Botao:
            text: 'próximo'
            on_press: root.manager.current = 'tela4'

<Tela4>:
    name: 'tela4'
    Default:
        Pergunta:
            title: "Quem seria o seu candidado a vereador, nestas eleições?"

        CaixaTexto:
            pos_hint: {'center_x':0.5, 'center_y':0.5}

        Botao:
            text: 'próximo'
            on_press: root.manager.current = 'tela5'

<Tela5>:
    name: 'tela5'
    Default:
        Pergunta:
            title: "Como o Sr(a) avalia o mandato do prefeito atual?"

        Opcao:
            pos_hint: {'center_x':0.2, 'center_y':0.75}

        MDLabel:
            text: 'Aprova'
            pos_hint: {'center_x':0.79, 'center_y':0.75}

        Opcao:
            pos_hint: {'center_x':0.50, 'center_y':0.75}

        MDLabel:
            text: 'Desaprova'
            pos_hint: {'center_x':1.1, 'center_y':0.75}


        Botao:
            text: 'próximo'
            on_press: root.manager.current = 'tela1'


